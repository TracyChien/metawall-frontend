// const host = "https://afternoon-earth-74578.herokuapp.com";
const host = " http://localhost:3000";

const config = {
  post_url: `${host}/posts`,
  sign_in_url: `${host}/users/sign_in`,
  sign_up_url: `${host}/users/sign_up`,
  get_profile_url: `${host}/users/profile`,
};

export default config;
